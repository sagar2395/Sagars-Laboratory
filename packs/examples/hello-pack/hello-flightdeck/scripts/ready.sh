#!/usr/bin/env bash
set -euo pipefail
# A no-op stage script: scenarios can be purely check-driven. This exists so the
# example demonstrates a 'script' component. Replace with real setup as needed.
echo "hello-flightdeck: nothing to install — this scenario only verifies go-api."
